// Site-level constants for the public web app.
// Publisher name used in Article JSON-LD. Not a domain — avoids hardcoding
// production URLs (those come from NEXT_PUBLIC_SITE_URL).
export const PUBLISHER_NAME = 'Emoji Platform';
